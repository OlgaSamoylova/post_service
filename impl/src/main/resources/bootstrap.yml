#spring:
#  application:
#    name: microservice-post
#
#  cloud:
#    kubernetes:
#      secrets:
#        sources:
#          - name: microservice-post
#      config:
#        sources:
#          - name: library-core
#          - name: microservice-post
#      reload:
#        enabled: true

spring:
  application:
    name: microservice-post
  cloud:
    kubernetes:
      enabled: false
      loadbalancer:
        enabled: false
  config:
    activate:
      on-profile: local, test

management:
  endpoint:
    restart:
      enabled: true
    health:
      enabled: true
    info:
      enabled: true
    prometheus:
      enabled: true
